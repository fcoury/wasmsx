{
  "name": "wasmsx",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.0.1",
    "live-server": "^1.2.2",
    "typescript": "^5.0.4"
  },
  "scripts": {
    "build": "tsc",
    "serve": "live-server --port=8080",
    "dev": "concurrently \"tsc -w\" \"live-server --port=8080\"",
    "build:wasm": "cd .. && wasm-pack build --target web --out-dir client/pkg"
  }
}
